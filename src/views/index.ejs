<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PhotoApp</title>
    <%- include('partials/head') %>
    <style>
        .bg-custom-gradient {
          background: linear-gradient(to right, #95d2e6, #f9f6fb);
        }
      </style>
</head>
<body class="bg-custom-gradient">
    <%- include("partials/navigation")  %>
    
    <header>
        <div class="container text-center mt-4">
            <h1>PhotoApp</h1>
            <p>Administra tus imágenes de manera fácil y divertida</p>
        </div>
    </header>

     <div class="container p-4">
        <div class="row">
            <div class="card-columns">
                <% images.reverse().forEach(function(image){%>
                    <div class="card animate__animated animate__fadeInUp">
                        <img src="<%= image.path %>" class="card-img-top">
                        <div class="card-body">
                            <h5 class="card-title"><%= image.title %> </h5>
                            <p class="card-text"><%= image.description %></p>
                            <p class="card-text">
                                <!-- Moment para formatear el tiempo -->
                                <%= moment(image.created_at).fromNow() %>
                            </p>
                            <a href="/image/<%= image._id %>" class="btn btn-primary">
                                 View Profile
                            </a>

                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
     </div> 
</body>
</html>